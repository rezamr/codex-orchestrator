# Renderer

Vue 3 desktop UI.

The renderer displays projections of application state and requests operations through the preload bridge. It does not spawn processes, access SQLite, call OS power APIs, or communicate with Codex app-server directly.

Follow `docs/ui-ux-guidelines.md`.
